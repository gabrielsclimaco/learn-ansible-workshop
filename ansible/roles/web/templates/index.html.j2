<html>
  <title>Ansiblezada</title>

  <body>
    <h1>{{ page_title }}</h1>
    <img src="{{ gif_url }}" />
  </body>

  <style>
    body {
      height: 100vh;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
    }

    img {
      width: 40vw;
      height: auto;
    }
  </style>
</html>
